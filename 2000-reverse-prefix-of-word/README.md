# 2000. Reverse Prefix of Word

---

![Difficulty](https://img.shields.io/badge/Difficulty-Easy-brightgreen)  ![Language](https://img.shields.io/badge/Language-C%2B%2B-blue)

---

## Problem

https://leetcode.com/problems/reverse-prefix-of-word/

---

## Language

C++

---

## Explanation

Initially:

stack = []
i = 0
isthere = false
c = 'a'
st.push('a');
i++;

Now:

stack:
top → a

i = 1

a != d, continue.

c = 'b'

Push b:

stack:
top → b
      a

i = 2

b != d, continue.

c = 'c'

Push c:

stack:
top → c
      b
      a

i = 3

c != d, continue.

c = 'd'

Push d:

stack:
top → d
      c
      b
      a

Then:

i++;

so:

i = 4

Now:

if(c == ch)

d == d, so:

isthere = true
break

The loop stops.

At this point:

stack = [a,b,c,d]
top = d

i = 4
isthere = true

Notice we didn't process e, because we broke immediately when we found d.

Second loop

Now:

for(int j = 0; j < i; j++){
    word[j] = st.top();
    st.pop();
}

Since:

i = 4

the loop runs for:

j = 0,1,2,3
j = 0

Top is d.

word[0] = 'd';
st.pop();

So:

word = "dbcde"

Stack:

top → c
      b
      a
j = 1

Top is c.

word[1] = 'c';
st.pop();

Now:

word = "dccde"

Stack:

top → b
      a
j = 2

Top is b.

word[2] = 'b';
st.pop();

Now:

word = "dcbde"

Stack:

top → a
j = 3

Top is a.

word[3] = 'a';
st.pop();

Now:

word = "dcbae"

Stack is empty.

Finally:

return word;

returns:

"dcbae"
Whole thing visually

Original:

a b c d e
↑──────↑
 prefix

You push until d:

stack:

d
c
b
a

Then pop:

d → word[0]
c → word[1]
b → word[2]
a → word[3]

So:

a b c d e
↓ ↓ ↓ ↓
d c b a e

= "dcbae"
One thing your code is doing nicely

You use:

if(!isthere) return word;

so if ch doesn't exist, you simply return the original string and don't try to pop from the stack.

For example:

word = "abcde"
ch = 'x'

You push all characters, never find x, and return:

"abcde"

Your logic is correct.

---

---

*Generated by LeetSync v1.0.0 — 2026-09-23 09:45 UTC*